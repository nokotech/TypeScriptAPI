import * as assert from 'assert';


describe("Test Case1", () => {
    it("test 1", () => {
        assert.ok(true, "this shouldn't fail")
    })
    it("test 2", () => {
        assert.ok(false, "this shouldn't fail")
    })
})